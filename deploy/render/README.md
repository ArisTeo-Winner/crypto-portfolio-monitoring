# Render environment templates

This folder mirrors the root-level Render templates for visibility in IDEs/file explorers.

- `.env.render-staging`
- `.env.render-prod`

Keep these files synchronized with the copies in the repository root.
